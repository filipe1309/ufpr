
rails new trabRails5 --skip-bundle
cd trabRails5
bundle install --local
//Criando schemas das tabelas
rails generate scaffold User login:string password:string admin:boolean
rails generate scaffold Store name:string adress:string
rails generate scaffold Book title:string author:string
rails generate scaffold Edition title:string book_id:integer number:integer
rails generate scaffold Code name:string book_id:integer 
rails generate scaffold Books_stores book_id:integer store_id:integer
//Criando tabelas
rake db:migrate



/config/routes.rb 
get "log_out" => "sessions#destroy", :as => "log_out"
        get "log_in" => "sessions#new", :as => "log_in"
        get "sign_up" => "users#new", :as => "sign_up"
        root :to => "users#new"
  resources :users
  resources :sessions

Copiar para o projeto local:
app/views/sessions/
app/controllers/sessions_controller.rb

app/views/users/
app/models/user.rb
app/controllers/users_controller.rb

app/views/layouts/application.html.erb 
app/controllers/application_controller.rb

Modificar no projeto local:
Em /config/routes.rb
	Adicionar:
	get "log_out" => "sessions#destroy", :as => "log_out"
        get "log_in" => "sessions#new", :as => "log_in"
        get "sign_up" => "users#new", :as => "sign_up"
        root :to => "users#new"
  	resources :users
  	resources :sessions

Em app/views/layouts/application.html.erb
--> Ajustar Nomes e paths.

Em app/models/*.rb
--> Adicionar relações.

Em app/views/*edit.html.erb
--> Adicionar no inicio
<% if current_user.admin %>
...
--> Adicionar no fim:
<% else %>
        <p>Acesso Proibido!</p>
<% end %>

Em app/views/*index.html.erb
--> Modificar links do Edit e Destroy
...
        <td><%= link_to_if(current_user.admin,  'Edit', edit_departamento_path(departamento) )%></td>
        <td><%= link_to_if(current_user.admin,  'Destroy', departamento, method: :delete, data: { confirm: 'Você esta certo disto?' } )%></td>
...
<%= link_to_if(current_user.admin, 'New Departamento', new_departamento_path) %>
